name: on_demand_soak_test
on:
  workflow_dispatch:
    inputs:
      cl_image_tag:
        description: Chainlink image tag
        required: true
        default: develop
        type: string
      cl_image_ecr:
        description: Name of the AWS ECR repository containing the Chainlink image (without AWS account and region, e.g. "chainlink")
        required: false
        type: string
        default: chainlink

env:
  CONTRACT_ARTIFACTS_PATH: contracts/target/deploy
  MOD_CACHE_VERSION: 1

jobs:
  get_projectserum_version:
    name: Get ProjectSerum Version
    environment: integration
    runs-on: ubuntu-latest
    outputs:
      projectserum_version: ${{ steps.psversion.outputs.projectserum_version }}
    steps:
      - name: Checkout the repo
        uses: actions/checkout@v4
      - name: Get ProjectSerum Version
        id: psversion
        uses: ./.github/actions/projectserum_version

  e2e_custom_build_artifacts:
    name: E2E Custom Build Artifacts
    environment: integration
    permissions:
      id-token: write
      contents: read
    runs-on: ubuntu-latest-32cores-128GB # ghv-ignore!
    needs: [get_projectserum_version]
    steps:
      - name: Checkout the repo
        uses: actions/checkout@v4
      - name: Build contracts
        uses: ./.github/actions/build_contract_artifacts
        with:
          runner-os: ${{ runner.os }}
          image: backpackapp/build
          image-version: ${{ needs.get_projectserum_version.outputs.projectserum_version }}

  e2e_soak_test:
    name: E2E Soak Test (${{ matrix.variant }})
    environment: integration
    permissions:
      id-token: write
      contents: read
    runs-on: ubuntu-latest-16cores-64GB # ghv-ignore!
    timeout-minutes: 300
    strategy:
      fail-fast: false
      matrix:
        include:
          - variant: embedded
            configs: env.toml,products/solana/basic.toml,products/solana/soak.toml
          - variant: plugins
            configs: env.toml,products/solana/basic.toml,products/solana/soak.toml,products/solana/plugins.toml
    needs: [e2e_custom_build_artifacts]
    steps:
      - name: Checkout the repo
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@010d0da01d0b5a38af31e9c3470dbfdabdecca3a # v4.0.1
        with:
          aws-region: ${{ secrets.QA_AWS_REGION }}
          role-to-assume: ${{ secrets.QA_AWS_ROLE_TO_ASSUME }}
          role-duration-seconds: 1800
          mask-aws-account-id: true

      - name: Authenticate to ECR
        uses: aws-actions/amazon-ecr-login@062b18b96a7aff071d4dc91bc00c4c1a7945b076 # v2.0.1
        with:
          registries: ${{ secrets.QA_AWS_ACCOUNT_NUMBER }}
        env:
          AWS_REGION: ${{ secrets.QA_AWS_REGION }}

      - name: Install Solana CLI
        run: ./scripts/install-solana-ci.sh

      - name: Install gauntlet
        uses: ./.github/actions/build-gauntlet

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: integration-tests/go.mod
          cache: true
          cache-dependency-path: integration-tests/go.sum

      - name: Download Go dependencies
        working-directory: integration-tests
        run: go mod download

      - name: Download contract artifacts
        uses: actions/download-artifact@v4
        with:
          name: artifacts
          path: ${{ env.CONTRACT_ARTIFACTS_PATH }}

      - name: Start fakes server
        working-directory: integration-tests/devenv
        run: |
          go build -o /tmp/fakes-server ./fakes/cmd
          /tmp/fakes-server &
          for _ in {1..20}; do
            if curl -fsS -X POST "http://127.0.0.1:9111/mockserver-bridge" > /dev/null; then
              echo "Fakes server is ready"
              exit 0
            fi
            sleep 1
          done
          echo "Fakes server failed to start"
          exit 1

      - name: Setup Environment
        working-directory: integration-tests/devenv
        env:
          CHAINLINK_IMAGE: ${{ secrets.QA_AWS_ACCOUNT_NUMBER }}.dkr.ecr.${{ secrets.QA_AWS_REGION }}.amazonaws.com/${{ github.event.inputs.cl_image_ecr }}:${{ github.event.inputs.cl_image_tag }}
        run: go run ./cmd u ${{ matrix.configs }} && go run ./cmd obs up -f

      - name: Run Soak Test
        working-directory: integration-tests/tests
        run: go test -v -timeout 4h -run TestSolanaOCRV2Soak

      - name: Show Docker containers status
        if: failure()
        run: docker ps -a

      - name: Upload Logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: soak-test-logs-${{ matrix.variant }}
          path: |
            integration-tests/devenv/logs/
            integration-tests/tests/logs/
          retention-days: 7
